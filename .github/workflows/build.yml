name: Build Datavyu

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-13]

    env:
      MAVEN_FLAGS: "-B -ntp"
      MAVEN_OPTS: >-
        -Xmx2g
        -Dfile.encoding=UTF-8
        -Dswing.aatext=true
        -Dawt.useSystemAAFontSettings=lcd
        -Dorg.apache.maven.wagon.httpconnectionManager.ttl=120000
        -Dorg.apache.maven.wagon.http.retryHandler.count=2

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: '8'
          cache: 'maven'
      - name: Show remote repositories
        run: mvn -s "$HOME/.m2/settings.xml" -B help:effective-pom | sed -n '/<repositories>/,/<\/repositories>/p'

      - name: Set up Maven mirrors (Central only; block slow rubygems)
        run: |
          mkdir -p "$HOME/.m2"
          cat > "$HOME/.m2/settings.xml" <<'XML'
          <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
                    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                    xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 https://maven.apache.org/xsd/settings-1.0.0.xsd">
            <mirrors>
              <!-- 只镜像 Central 到阿里云 -->
              <mirror>
                <id>aliyun-central</id>
                <mirrorOf>central</mirrorOf>
                <name>Aliyun Central</name>
                <url>https://maven.aliyun.com/repository/central</url>
              </mirror>
              <!-- 可选：jcenter/gradle-plugin 也走阿里云（按需保留） -->
              <mirror>
                <id>aliyun-public</id>
                <mirrorOf>jcenter,gradle-plugin</mirrorOf>
                <name>Aliyun Public</name>
                <url>https://maven.aliyun.com/repository/public</url>
              </mirror>
              <!-- 关键：让 POM 里的 rubygems-releases 不再命中 otto.takari.io（直接映射到 Central，快速失败） -->
              <mirror>
                <id>rubygems-fast-fail</id>
                <mirrorOf>rubygems-releases</mirrorOf>
                <name>Map rubygems-releases to Central</name>
                <url>https://repo1.maven.org/maven2</url>
              </mirror>
            </mirrors>
          </settings>
          XML


      - name: Preinstall SDL libs (macOS)
        if: runner.os == 'macOS'
        run: |
          mvn -s "$HOME/.m2/settings.xml" $MAVEN_FLAGS install:install-file \
            -Dfile=vendor/sdl-libs-2.0.9-mac.jar \
            -DgroupId=org.datavyu \
            -DartifactId=sdl-libs \
            -Dversion=2.0.9 \
            -Dpackaging=jar \
            -Dclassifier=mac

      - name: Show effective POM
        run: mvn -s "$HOME/.m2/settings.xml" $MAVEN_FLAGS -U -DforceStdout help:effective-pom

      - name: Show dependency tree
        run: mvn -s "$HOME/.m2/settings.xml" $MAVEN_FLAGS -U dependency:tree -DoutputType=text

      - name: Build native installers
        run: mvn -s "$HOME/.m2/settings.xml" $MAVEN_FLAGS -Dmaven.test.skip=true -U clean jfx:native

      - uses: actions/upload-artifact@v4
        with:
          name: datavyu-${{ runner.os }}
          path: |
            target/jfx/native/**/*
            target/*.jar
